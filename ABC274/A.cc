// cout<<fixed<<setprecision(10)<<endl;
#include <bits/stdc++.h>

using namespace std;

int main() {
  double a, b;
  cin >> a >> b;
  cout << fixed << setprecision(3) << b / a << endl;

  return 0;
}