#include <bits/stdc++.h>
using ll = long long int;
#define RES 2000000;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;
int main (){
		cin.tie(0);
  	ios_base::sync_with_stdio(false);
   int n=1;
	do{
		cout<<n;
		n++;
	}while(n<1);
	 return 0;
}
